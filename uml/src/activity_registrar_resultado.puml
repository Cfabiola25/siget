@startuml
title Actividad: Registrar Resultado
start
:Recibir matchId y goles;
if (Partido existe?) then (sí)
  if (Estado == scheduled?) then (sí)
    :Validar goles >= 0;
    :Actualizar estado a played;
    :Guardar homeGoals y awayGoals;
    :Confirmar;
  else (no)
    :Rechazar - solo partidos programados;
  endif
else (no)
  :Error - partido no encontrado;
endif
stop
@enduml